<?xml version="1.0" encoding="UTF-8"?>
<persistence xmlns="https://jakarta.ee/xml/ns/persistence"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="https://jakarta.ee/xml/ns/persistence https://jakarta.ee/xml/ns/persistence/persistence_3_1.xsd"
             version="3.1">

    <persistence-unit name="NexcellPU" transaction-type="RESOURCE_LOCAL">
        <provider>org.hibernate.jpa.HibernatePersistenceProvider</provider>

        <class>modelo.Usuario</class>
        <class>modelo.Admin</class>
        <class>modelo.Gerente</class>
        <class>modelo.Vendedor</class>

        <properties>
            <!-- Conexión a MySQL (Modificar con credenciales locales) -->
            <property name="jakarta.persistence.jdbc.driver" value="com.mysql.cj.jdbc.Driver"/>
            <property name="jakarta.persistence.jdbc.url" value="jdbc:mysql://localhost:3306/nexcell_db?serverTimezone=UTC"/> <!-- En caso de ser necesario, aqui podras cambiar el puerto de 3306 -->
            <property name="jakarta.persistence.jdbc.user" value="TU_USUARIO_AQUI"/> <!-- Aqui pondras tu nombre de usuario -->
            <property name="jakarta.persistence.jdbc.password" value="TU_CONTRASEÑA_AQUI"/> <!-- Aqui pondras tu contraseña -->

            <!-- Configuración de Hibernate -->
            <property name="hibernate.dialect" value="org.hibernate.dialect.MySQLDialect"/>
            <property name="hibernate.show_sql" value="true"/>
            <property name="hibernate.format_sql" value="true"/>

            <!-- update: Actualiza el esquema automáticamente basado en tus clases Java -->
            <property name="hibernate.hbm2ddl.auto" value="update"/>
        </properties>
    </persistence-unit>
</persistence>